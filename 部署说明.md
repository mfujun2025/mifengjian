# 密封件.cn 站点交付说明

交付日期：2026-09-14　｜　站点域名：**密封件.cn**（punycode：`xn--5nq252acf.cn`）

---

## 一、交付清单

| 交付物 | 位置 | 说明 |
|---|---|---|
| 可部署站点 | `public/` | 16 个页面，纯静态，直接丢到 GitHub Pages 就能跑 |
| O 型圈规格查询工具 | `public/tool/index.html` | 单文件纯前端，77 KB，零后端依赖 |
| 规格数据集 | `data/o-ring-specs.json` | 305 条 GB/T 3452.1-2005 G 系列规格 |
| 材质与介质规则 | `data/materials.json`、`data/media.json` | 10 种材质、20 类介质兼容规则 |
| 文章源码 | `content/*.md` | 8 篇原创内容，Markdown + front matter |
| 构建脚本 | `build.py`、`gen_data.py` | 数据生成 + 站点构建，可重复执行 |
| 页面模板 | `templates/layout.html`、`templates/tool.html` | 站点外壳 + 工具页模板 |
| 自动部署 | `public/.github/workflows/deploy.yml` | push 到 main 自动构建并发布 |
| 飞书中转（可选） | `worker/feishu-form-proxy.js` | Cloudflare Worker 源码，用于隐藏 webhook 地址 |
| 询单接收端一键配置 | `配置询单接收端.bat`、`setup_form.py` | 双击运行：粘 webhook → 真发测试卡片 → 写入配置 → 自动重建 |
| 接收端配置 | `config.local.json` | 本地填 webhook 的地方（已加入 `.gitignore`，不进仓库） |
| 表单回归测试 | `tests/mock_feishu.py`、`tests/mktest.py` | 本地模拟飞书端点，改完表单可一键复测 |
| 部署验证截图 | `../shots/`、`../shots2/` | 页面实测截图（含询单表单各状态） |

---

## 二、站点结构

```
public/
├── index.html                     首页
├── tool/index.html                O 型圈规格查询工具（核心）
├── materials/index.html           材质百科列表
├── articles/index.html            技术文章列表
├── articles/<slug>/index.html    8 篇文章详情页
├── faq/index.html                 常见问题（含 FAQ 结构化数据）
├── contact/index.html             询单表单
├── about/index.html               关于本站 + 免责声明
├── 404.html                       404 页面
├── robots.txt / sitemap.xml       SEO
├── CNAME                          GitHub Pages 自定义域名
├── style.css                      全站样式
└── data/*.json                    数据文件（供二次开发复用）
```

**共 16 个 HTML 页面**：1 首页 + 1 工具页 + 2 列表页 + 8 文章页 + 1 FAQ + 1 询单 + 1 关于 + 1 个 404。

---

## 三、工具页的三个功能

工具页是本次交付的核心资产，纯前端、无后端、可离线使用。

### 1. 尺寸查询
输入内径 d1、截面 d2、外径 d3 中任意一项或组合 → 命中标准规格，自动带出内径公差、截面公差、外径。
外径按 `d3 = d1 + 2 × d2` 计算，不用手算。无命中时给出邻近标准内径建议。

### 2. 近似匹配
输入旧件实测内径与实测截面 → 按接近度排序给出前 12 个标准规格，带匹配度百分比。

匹配度算法：`100 − (|Δ内径| × 1 + |Δ截面| × 3) × 12`
截面偏差权重更高，因为截面不对会直接破坏压缩率。

实测：`19.8 × 2.7` → 推荐 `20×2.65`，匹配度 **96%**。

### 3. 材质选型
输入工作温度 + 接触介质 + 运动方式 → 输出四组结果：

- **推荐材质**（介质明确耐、温度合适）
- **可选材质**（次选或需确认，含"接近耐温边界"提示）
- **需实测验证**（介质相容性数据不足）
- **已排除材质**，每条写明排除原因

排除逻辑：温度越界 → 排除；介质命中黑名单 → 排除并给出具体原因。
附带输出该工况的推荐硬度与压缩率。

实测：`150℃ + 液压油` → 推荐 FKM（首选）/ FFKM / PTFE，可选 HNBR（已提示接近耐温上限），
正确排除 NBR（超温）、EPDM（遇矿物油溶胀）、SI（硅橡胶不耐矿物油）、PU（超温）、CR（超温）。

### URL 参数直达（新增能力）
结果可以分享和收藏：

```
/tool/?d1=20&d2=2.65                          尺寸查询
/tool/?n1=19.8&n2=2.7                         近似匹配
/tool/?t=150&media=hydraulic_mineral          材质选型
```

---

## 四、本地预览

```bash
cd seal-cn-site
python -m http.server 8791 --directory public
# 浏览器打开 http://127.0.0.1:8791/
```

修改内容后重新构建：

```bash
python gen_data.py     # 改了规格/材质数据才需要跑
python build.py        # 生成 public/
```

> 注意：`build.py` 采用覆盖式构建，不会清空 `public/`。若删除了某篇文章，需要手动删除对应的 `public/articles/<slug>/` 目录。

---

## 五、部署到 GitHub Pages

### 第 1 步：建仓库并上传

1. GitHub 新建仓库，选 **Public**（免费版 Pages 要求公开仓库，或账号升级 Pro）
2. 把 `seal-cn-site/` 整个目录上传（含 `public/`、`build.py`、`content/`、`data/`）
3. 仓库 **Settings → Pages → Source** 选 **GitHub Actions**

### 第 2 步：自定义域名

1. Settings → Pages → Custom domain 填 `密封件.cn`（GitHub 会自动转成 punycode）
2. DNS 处添加记录：

| 类型 | 主机记录 | 记录值 |
|---|---|---|
| CNAME | @ | 你的用户名.github.io |
| CNAME | www | 你的用户名.github.io |

> `CNAME` 文件已生成在 `public/CNAME`，内容为 `xn--5nq252acf.cn`，不要改。

3. 等 DNS 生效（一般 10 分钟~2 小时），回到 Pages 勾选 **Enforce HTTPS**

### 第 3 步：接 Cloudflare（可选，但推荐）

**⚠️ 关键一步：接上 Cloudflare 后，务必关掉 Managed robots.txt。**

路径：Cloudflare → 你的域名 → **缓存** → **配置** → 找到 **Managed robots.txt** → 关闭。

不关的话，Cloudflare 会自动注入它自己的 `robots.txt`，导致你站点的 `robots.txt` 和 sitemap 声明永远不生效。

其他建议：
- SSL/TLS 模式选 **Full (strict)**
- 打开 **Always Use HTTPS**
- 缓存级别用 **Standard**，不要对 HTML 做激进缓存

### 第 4 步：验证清单

部署完成后逐项检查：

- [ ] `https://密封件.cn/` 首页可访问
- [ ] `https://密封件.cn/tool/` 工具页可打开，三个功能都能算出结果
- [ ] `https://密封件.cn/robots.txt` 显示的是**你自己**的内容（含 Sitemap 声明）
- [ ] `https://密封件.cn/sitemap.xml` 可访问，含全部页面
- [ ] `https://密封件.cn/404.html` 正常（GitHub Pages 会自动用 404.html）
- [ ] 手机打开工具页，布局不溢出
- [ ] 提交百度/必应站长平台，提交 sitemap

---

## 六、询单表单 → 飞书群机器人

表单已经接好飞书群机器人。**填一个地址就生效，不需要后端服务、不需要服务器、零月费**。原理是飞书 webhook 接口本身开了 CORS（实测返回 `Access-Control-Allow-Origin: *`），浏览器可以直接 POST，而且响应体读得到——能给访客真实的成功/失败回执，不是黑盒提交。

### 先说清楚：webhook 是个什么东西

一句话：**一个只属于某个群的、专门用来往里发消息的网址。**

普通网址是你去访问它、拿回内容；webhook 是反过来的——你把消息 POST 过去，它就替你把消息发进群里。飞书给每个群里的「自定义机器人」分配一个这样的网址：

```
https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

谁拿到这个网址（加上它设的安全校验），谁就能往这个群发消息，所以它算半个密码——别往公开仓库里放。

### 第 1 步：拿到 webhook（约 1 分钟）

接收群已经建好了：**「密封件.cn 询单」**，只有你一个人，owner 是你。
飞书里直接找这个群，或者点：
<https://applink.feishu.cn/client/chat/open?openChatId=oc_a8be50ecd8bdf4e7ce732127f78689b3>

1. 进入「密封件.cn 询单」群 → 右上角 **···** → **设置**
2. 右侧设置面板 → **群机器人** → **添加机器人** → 选 **自定义机器人**
3. 名称填「密封件.cn 询单」，描述随意 → **添加**
4. **安全设置勾「自定义关键词」，关键词填 `询单`** ← 这一步别选错
5. 点 **完成**，复制生成的 webhook 地址

> **为什么必须选「自定义关键词」，不能用「签名校验」？**
> 这是纯静态站，签名用的 secret 只能写在前端 JS 里，等于公开——别人拿到就能伪造询单，比不签名更糟。
> IP 白名单同样不行：GitHub Pages 的出口 IP 不固定。
> 关键词校验不暴露任何密钥，且 webhook 即使被人看到，不知道关键词也发不进来。**关键词是唯一正确的选择。**

### 第 2 步：填进配置（双击一个文件就行）

**做法 A（推荐）**：双击站目录下的 **`配置询单接收端.bat`**，把 webhook 粘进去、回车。

脚本会：校验格式 → **真发一条测试卡片到你群里**（你能立刻看到实际效果）→ 写入 `config.local.json` → 自动重建站点。
失败时会直接告诉你飞书返回的错误码是什么意思，不用自己猜。

**做法 B（手动）**：编辑 `config.local.json`

```json
{
  "feishu_webhook": "https://open.feishu.cn/open-apis/bot/v2/hook/你的token",
  "feishu_keyword": "询单",
  "form_endpoint": ""
}
```

然后重新构建：`python build.py`

配置优先级：`config.local.json` > 环境变量 > 空（演示模式）。
临时测试也可以不改文件，直接用环境变量：

```bash
# Windows PowerShell
$env:SEAL_FEISHU_WEBHOOK="https://open.feishu.cn/..."; python build.py
# macOS / Linux
SEAL_FEISHU_WEBHOOK="https://open.feishu.cn/..." python build.py
```

**如果用 GitHub Actions 自动部署（推荐）**，把地址放进仓库 Secrets，代码里不出现明文：
**仓库 → Settings → Secrets and variables → Actions → New repository secret**
- Name：`FEISHU_WEBHOOK`
- Secret：你的 webhook 地址

构建工作流已经读这个 Secret（见 `.github/workflows/deploy.yml`），push 一次就自动带上。

> `config.local.json` 已写进 `.gitignore`，不会提交到仓库。

### 第 3 步：验证

构建时会打印当前生效的接收端，确认没配错：

```
询单接收端：飞书群机器人（浏览器直连）  https://open.feishu.cn/open-apis/bot/v2/hook/xxx
```

然后打开站点的「选型咨询」页，随便填一条提交。群里应该马上收到一张卡片：

```
🔔 新询单 · 密封件.cn
称呼：张工程师
联系方式：138xxxx
规格/尺寸：20×2.65
数量：500 只/月
工作温度：长期 120，峰值 150
接触介质：矿物液压油
补充说明：压力 16MPa，往复运动，需要耐油认证
——
来源：选型咨询页 · 2026-09-15 06:42 · 类型：询单
```

### 收不到消息？按这个顺序排查

| 页面提示 | 原因 | 处理 |
|---|---|---|
| 接收端返回异常：`incoming webhook access token invalid` | webhook 地址错，或机器人被删了 | 重新复制地址 |
| 接收端返回异常：`key words not found` | 机器人关键词和消息里的对不上 | 关键词改成 `询单`；或改 `FEISHU_KEYWORD` 与之一致并重新构建 |
| 接收端返回异常：`sign not match` | 安全设置选了「签名校验」 | 改成「自定义关键词」 |
| 提交失败：网络或接收端异常 | 浏览器插件或公司网络拦了跨域请求 | 换浏览器/换网络复测 |
| 页面正常、群里没消息 | 机器人被移出群，或被停用 | 检查群机器人列表 |

### 已内置的防护（三层）

纯静态站直连 webhook，地址必然会出现在前端源码里。所以做了三层处理：

| 措施 | 位置 | 作用 |
|---|---|---|
| 自定义关键词 | 飞书机器人设置 | 拿到地址也必须带关键词才能发，挡掉扫描脚本 |
| 蜜罐字段 | 前端（不可见、不可聚焦、`autocomplete=off`） | 只有机器人会填；命中后**假装成功但不发送**，不暴露拦截规则 |
| 来源校验 + IP 限流 + 字段裁剪 | Cloudflare Worker（可选） | 只允许自己域名调用，同 IP 每分钟最多 5 次，字段长度截断 |

### 可选加固：改用 Cloudflare Worker 中转

不想让 webhook 暴露在前端就走这条路，5 分钟搞定，免费额度 10 万次/天，够用：

1. Cloudflare Dashboard → **Workers & Pages** → 创建 Worker
2. 把 `worker/feishu-form-proxy.js` 的内容整体粘进去，保存并部署
3. Worker 的 **Settings → Variables and Secrets** 添加加密变量：
   - `FEISHU_WEBHOOK` = 你的飞书 webhook 地址
   - （可选）`ALLOW_ORIGINS` = `https://xn--5nq252acf.cn,https://www.xn--5nq252acf.cn`
4. 拿到 Worker 地址（形如 `https://xxx.yyy.workers.dev`），填进 `config.local.json` 的 `form_endpoint`：
   ```json
   { "feishu_webhook": "", "feishu_keyword": "询单", "form_endpoint": "https://xxx.yyy.workers.dev" }
   ```
   重新构建。**两个地址同时存在时优先走飞书直连**；只想用 Worker 就把 `feishu_webhook` 留空。

### 不配置会怎样

两个地址都留空时，表单进入**演示模式**：前端校验照跑，点提交提示「尚未配置接收端」，同时给访客一个「复制这份内容」按钮。**不会把数据发给任何第三方**，站点也不会报错。

### 顺带的转化闭环

工具页的材质选型结果底部，新增了「**按此工况询价 →**」按钮，会把当前选的温度和介质带进询单页并自动填好（实测：选 `150℃ + 矿物液压油` → 跳过去后规格/温度/介质都有值，还提示"已从规格查询工具带入参数"）。访客不用重新填一遍，询单页的字段完整度明显提高。

---

## 六之二、表单的本地回归测试

改过表单逻辑后想验证没跑坏，可以跑这套（需要本机有 Python 和 Chrome）：

```bash
python tests/mock_feishu.py          # 终端 A：起一个模拟飞书端点（含 CORS，与飞书行为一致）
SEAL_FEISHU_WEBHOOK="http://127.0.0.1:8792/hook" python build.py   # 终端 B
python tests/mktest.py               # 生成测试页 public/_t_form.html
# 再用无头浏览器依次访问，看页面里的 RESULT 行
#   /_t_form.html?c=ok       期望 okShown=true
#   /_t_form.html?c=fail     期望 errShown=true（模拟飞书报错）
#   /_t_form.html?c=required 期望 errShown=true（必填拦截）
#   /_t_form.html?c=honeypot 期望 okShown=true 且 mock 端收到 0 条请求
#   /_t_form.html?c=prefill  期望预填生效
```

`_form_requests.jsonl` 是 mock 端收到的请求原文，可以直接核对发出的飞书卡片 JSON 长什么样。测完记得删掉 `public/_t_form.html`，别部署上去。

---

## 七、内容后续规划

当前 8 篇。要起量，优先级建议：

**第一优先：补齐材质百科，还差 7 种**
HNBR、FVMQ（氟硅）、FFKM（全氟醚）、PTFE 包覆、PU（聚氨酯）、CR（氯丁）、SI 单独成篇。
理由：工具页已经有这 10 种材质的数据和结论，文章是它们的自然延伸，内链密度最高。

**第二优先：标准对照表**
GB ↔ JIS B 2401 ↔ AS568 逐规格对照。
理由：工程师天天查这个，但公开资料做得极差，竞争小、搜索意图精准。

**第三优先：应用场景页**
「挖掘机油封型号」「液压缸密封圈选型」这类词搜索精准、客户不砍价。
每个场景页 = 一张选型表 + 一段工况说明 + 一个询单入口。

**内容与工具的关系**：文章负责被搜索到，工具负责留住人，询单页负责转化。
每篇文章底部都已有工具入口，工具页底部有询单入口，链路已经通了。

---

## 八、已知限制

1. **规格数据是 G 系列（一般应用）**。GB/T 3452.1-2005 还有其他系列，本站只数字化了最常用的 G 系列 305 条组合。原文方案提到 241 条，实际按标准 G 系列完整展开是 305 条，覆盖更全。
2. **公差为标称值**。正式定型请以标准原文与厂家实测报告为准，站内已写明这一条。
3. **材质耐介质结论是工程归纳**，用于初筛。"需实测验证"不等于不能用，是需要先做相容性试验。
4. **表单已接飞书群机器人**，但需要在 `build.py` 填一次 webhook 地址才真实收单（见第六节）。未填时走演示模式，不丢数据给第三方。
5. **未做多语言**。当前全中文，符合域名定位。

---

## 九、技术选型说明

原方案规划用 Hugo。实际构建时本机环境装不上 Hugo（下载包被截断），因此改用**等价的静态构建方式**：

- `gen_data.py` 负责数据生成（Python）
- `build.py` 负责 Markdown → HTML、生成 sitemap/robots/结构化数据

产出物与 Hugo 完全一致（都是 `public/` 静态站），部署流程、目录结构、SEO 文件都不变。
好处是构建依赖只有 Python + markdown 一个库，GitHub Actions 里 `pip install markdown` 一行就能跑，比装 Hugo 更省事、更不容易失败。
